# Cluster Setup

Our Kubernetes cluster runs on the NixOS nodes. We use **Flux** for GitOps and **SOPS** for secret management.

<ServiceLinks repo="cluster-infra" />

The cluster configuration, including infrastructure components and application deployments, is defined in this repository.
