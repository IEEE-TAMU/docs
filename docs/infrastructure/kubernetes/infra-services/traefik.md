# Traefik

Traefik is used as the Ingress Controller for our cluster. It is set up by K3s/Rancher.

Traefik handles service ingress configuration, mostly managing traffic that is proxied through the Cloudflare Tunnel.
